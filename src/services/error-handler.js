const backendError = error => {
  if (error) {
    // you need to login again
  }
};

const fetchError = error => {
  if (error) {
    // you need to login again
  }
};

export { backendError, fetchError };
